#include "monty.h"
/**
 * stack - switch data format to stack
 * @structstack: address of first node
 * @lineno: line number
 *
 * Return: nothing
 */
void stack(stack_t **structstack, unsigned int lineno)
{
	(void) structstack;
	(void) lineno;
	num[0] = 0;
}

/**
 * queue - switch data format to queue
 * @structstack: address of first node
 * @lineno: line number
 *
 * Return: nothing
 */
void queue(stack_t **structstack, unsigned int lineno)
{
	(void) structstack;
	(void) lineno;
	num[0] = 1;
}
